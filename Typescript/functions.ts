function greet(name: string, age: number) {
  console.log(`Hello, my name is ${name} and my age ${age}`);
}
greet("Dana", 25);

// function checkBalance(amount: number) : Promise<number> {
const balance = 500;
//}//
